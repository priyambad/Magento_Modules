<h3>Comments</h3>
<div class="comments-container">
    <?php
    $comments = $block->getComments();
    if($comments){
        foreach ($comments as $key => $comment) {
        ?>
        <div class="comments">
            <p><strong><?php echo $comment['time'].", ( ".$comment['author']." )"?></strong></p>
            <p><?php echo $comment['content'] ?></p>
        </div>
        <hr>
        <?php
        }
    }else {
    ?>
        <div class="comments">
            <p>There are no comments</p>
        </div>
    <?php }?>
</div>
<div class="item actions">
    <a class="action back btn btn btn-outline-success btn-outline-green" href="<?= $block->getUrl('customer/forwarder/index') ?>"><span><?= $block->escapeHtml(__('Forwarders')) ?></span></a>
    <?php if ($block->canShowAction()) : ?>
        <a class="action edit btn btn btn-outline-success btn-outline-green" href="<?= $block->escapeUrl($block->getUrl('customer/forwarder/edit', ['id' => $block->getAddressId()])) ?>"><span><?= $block->escapeHtml(__('Edit Forwarder')) ?></span></a>
    <?php endif;?>
</div>
